# `conformance/`

The language-neutral valid, invalid and golden corpus that every binding must
agree on. TypeScript and Rust decoders are held to the same bytes and the same
error identities.

Owner: the contracts stream.
