name: Live User Tests

on:
  workflow_dispatch:
    inputs:
      artifact_source:
        description: SDK artifact to test.
        required: true
        default: pack-current
        type: choice
        options:
          - pack-current
          - published-version
      version:
        description: Published antpath version when artifact_source is published-version.
        required: false
        type: string
      run_heavy:
        description: Run the heavy full-feature live canary after the default live suite.
        required: true
        default: false
        type: boolean

concurrency:
  group: live-user-tests
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  live-user-tests:
    name: Live user tests
    runs-on: ubuntu-latest
    environment: live-user-tests
    timeout-minutes: 240
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Enable pnpm
        run: corepack enable

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Select SDK artifact
        id: artifact
        env:
          ARTIFACT_SOURCE: ${{ inputs.artifact_source }}
          PACKAGE_VERSION: ${{ inputs.version }}
        run: |
          if [ "$ARTIFACT_SOURCE" = "published-version" ]; then
            if [ -z "$PACKAGE_VERSION" ]; then
              echo "version input is required when artifact_source is published-version." >&2
              exit 1
            fi
            node scripts/cicd/wait-for-npm.mjs antpath "$PACKAGE_VERSION"
            echo "version=$PACKAGE_VERSION" >> "$GITHUB_OUTPUT"
          else
            pack_dir="$RUNNER_TEMP/antpath-pack"
            mkdir -p "$pack_dir"
            pnpm --filter antpath pack --pack-destination "$pack_dir"
            tarball="$(find "$pack_dir" -maxdepth 1 -name 'antpath-*.tgz' -print -quit)"
            if [ -z "$tarball" ]; then
              echo "No antpath tarball was produced." >&2
              exit 1
            fi
            echo "tarball=$tarball" >> "$GITHUB_OUTPUT"
          fi

      - name: Live user tests
        env:
          ANTPATH_API_URL: ${{ vars.ANTPATH_API_URL || vars.USER_TEST_ANTPATH_URL }}
          ANTPATH_API_TOKEN: ${{ secrets.ANTPATH_API_TOKEN || secrets.USER_TEST_API_TOKEN }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY || secrets.USER_TEST_DEEPSEEK_KEY }}
          ANTPATH_USER_TEST_MODEL: ${{ vars.ANTPATH_USER_TEST_MODEL || vars.ANTPATH_USER_TEST_DEEPSEEK_MODEL }}
          ANTPATH_USER_TEST_DEEPSEEK_MODEL: ${{ vars.ANTPATH_USER_TEST_DEEPSEEK_MODEL || vars.ANTPATH_USER_TEST_MODEL }}
          ANTPATH_USER_TEST_TARBALL: ${{ steps.artifact.outputs.tarball }}
          ANTPATH_USER_TEST_VERSION: ${{ steps.artifact.outputs.version }}
        run: pnpm run test:user

      - name: Heavy live user-test canary
        if: ${{ inputs.run_heavy }}
        env:
          ANTPATH_API_URL: ${{ vars.ANTPATH_API_URL || vars.USER_TEST_ANTPATH_URL }}
          ANTPATH_API_TOKEN: ${{ secrets.ANTPATH_API_TOKEN || secrets.USER_TEST_API_TOKEN }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY || secrets.USER_TEST_DEEPSEEK_KEY }}
          ANTPATH_USER_TEST_MODEL: ${{ vars.ANTPATH_USER_TEST_MODEL || vars.ANTPATH_USER_TEST_DEEPSEEK_MODEL }}
          ANTPATH_USER_TEST_DEEPSEEK_MODEL: ${{ vars.ANTPATH_USER_TEST_DEEPSEEK_MODEL || vars.ANTPATH_USER_TEST_MODEL }}
          ANTPATH_USER_TEST_TARBALL: ${{ steps.artifact.outputs.tarball }}
          ANTPATH_USER_TEST_VERSION: ${{ steps.artifact.outputs.version }}
        run: pnpm run test:user:heavy
