name: Promote

on:
  workflow_dispatch:
    inputs:
      version:
        description: Published @aexhq/sdk version to promote.
        required: true
        type: string
      release_run_id:
        description: Run ID of the green release.yml run that published this version.
        required: true
        type: string
      platform_deploy_run_id:
        description: Run ID of the green aex-platform deploy.yml run that tested this SDK version.
        required: true
        type: string
      npm_dist_tag:
        description: npm dist-tag to assign.
        required: true
        default: latest
        type: choice
        options:
          - latest
          - next
          - canary

concurrency:
  group: promote-aex
  cancel-in-progress: false

permissions:
  contents: read
  # Needed to read the release.yml run named by release_run_id.
  actions: read

jobs:
  promote:
    name: Promote @aexhq/sdk
    runs-on: ubuntu-latest
    environment: npm-release
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.14"

      # Fail-closed release proof: promotion requires the run ID of the
      # release.yml run that shipped this exact version. Via the GitHub API
      # (GITHUB_TOKEN) we verify that the run (a) is .github/workflows/
      # release.yml in THIS repo — the API path is repo-scoped, so a foreign
      # run id 404s; (b) completed with conclusion=success, which release.yml
      # only reaches after static gates, publish, and the published-artifact
      # smoke pass;
      # and (c) published THIS version — the npm registry publish timestamp
      # (`time[version]`) must fall inside that run's created_at..updated_at
      # window (created_at, not run_started_at, so re-run attempts of flaky
      # live shards don't move the window past the publish time). Any check
      # that cannot be evaluated fails the promotion.
      - name: Verify green release run published this version
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_RUN_ID: ${{ inputs.release_run_id }}
          PACKAGE_VERSION: ${{ inputs.version }}
        shell: bash
        run: |
          set -euo pipefail
          case "${RELEASE_RUN_ID}" in
            ''|*[!0-9]*)
              echo "::error::release_run_id must be a numeric workflow run id (got '${RELEASE_RUN_ID}')."
              exit 1
              ;;
          esac
          run_json="$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${RELEASE_RUN_ID}")"
          workflow_path="$(jq -r '.path' <<<"${run_json}")"
          status="$(jq -r '.status' <<<"${run_json}")"
          conclusion="$(jq -r '.conclusion' <<<"${run_json}")"
          run_created_at="$(jq -r '.created_at' <<<"${run_json}")"
          run_updated_at="$(jq -r '.updated_at' <<<"${run_json}")"
          if [ "${workflow_path}" != ".github/workflows/release.yml" ]; then
            echo "::error::Run ${RELEASE_RUN_ID} is '${workflow_path}', not .github/workflows/release.yml."
            exit 1
          fi
          if [ "${status}" != "completed" ] || [ "${conclusion}" != "success" ]; then
            echo "::error::Release run ${RELEASE_RUN_ID} has status='${status}' conclusion='${conclusion}'; promotion requires a fully green release run (published-artifact smoke included)."
            exit 1
          fi
          published_at="$(curl -fsSL "https://registry.npmjs.org/@aexhq%2Fsdk" | jq -r --arg v "${PACKAGE_VERSION}" '.time[$v] // empty')"
          if [ -z "${published_at}" ]; then
            echo "::error::@aexhq/sdk@${PACKAGE_VERSION} has no publish timestamp on npm; nothing to promote."
            exit 1
          fi
          start_s="$(date -u -d "${run_created_at}" +%s)"
          end_s="$(date -u -d "${run_updated_at}" +%s)"
          pub_s="$(date -u -d "${published_at}" +%s)"
          if [ "${pub_s}" -lt "${start_s}" ] || [ "${pub_s}" -gt "${end_s}" ]; then
            echo "::error::@aexhq/sdk@${PACKAGE_VERSION} was published at ${published_at}, outside release run ${RELEASE_RUN_ID}'s window (${run_created_at}..${run_updated_at}); that run did not publish this version."
            exit 1
          fi
          echo "Verified: green release run ${RELEASE_RUN_ID} published @aexhq/sdk@${PACKAGE_VERSION} at ${published_at}."

      - name: Verify public release manifest
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_RUN_ID: ${{ inputs.release_run_id }}
          PACKAGE_VERSION: ${{ inputs.version }}
        shell: bash
        run: |
          set -euo pipefail
          rm -rf public-release-manifest
          mkdir public-release-manifest
          gh run download "${RELEASE_RUN_ID}" \
            --repo "${GITHUB_REPOSITORY}" \
            --name "public-release-manifest-${RELEASE_RUN_ID}" \
            --dir public-release-manifest
          manifest_file="$(find public-release-manifest -name public-release-manifest.json -print -quit)"
          if [ -z "${manifest_file}" ]; then
            echo "::error::Release run ${RELEASE_RUN_ID} did not include public-release-manifest.json."
            exit 1
          fi
          bun scripts/cicd/release-manifest.mjs verify-public \
            --manifest "${manifest_file}" \
            --version "${PACKAGE_VERSION}" \
            --run-id "${RELEASE_RUN_ID}"

      # Fail-closed platform proof: promotion also requires a private
      # aex-platform deploy.yml run whose final Promotion proof job uploaded an
      # artifact naming this exact SDK version. That artifact is created only
      # after the dev suite, Spot canary, prd suite, and prod smoke are green.
      - name: Verify green platform deploy tested this version
        env:
          GH_TOKEN: ${{ secrets.PLATFORM_REPO_TOKEN }}
          PLATFORM_DEPLOY_RUN_ID: ${{ inputs.platform_deploy_run_id }}
          PACKAGE_VERSION: ${{ inputs.version }}
        shell: bash
        run: |
          set -euo pipefail
          if [ -z "${GH_TOKEN:-}" ]; then
            echo "::error::PLATFORM_REPO_TOKEN is required to verify the private aex-platform deploy proof before promotion."
            exit 1
          fi
          case "${PLATFORM_DEPLOY_RUN_ID}" in
            ''|*[!0-9]*)
              echo "::error::platform_deploy_run_id must be a numeric workflow run id (got '${PLATFORM_DEPLOY_RUN_ID}')."
              exit 1
              ;;
          esac
          run_json="$(gh api "repos/aexhq/aex-platform/actions/runs/${PLATFORM_DEPLOY_RUN_ID}")"
          workflow_path="$(jq -r '.path' <<<"${run_json}")"
          status="$(jq -r '.status' <<<"${run_json}")"
          conclusion="$(jq -r '.conclusion' <<<"${run_json}")"
          if [ "${workflow_path}" != ".github/workflows/deploy.yml" ]; then
            echo "::error::Platform run ${PLATFORM_DEPLOY_RUN_ID} is '${workflow_path}', not .github/workflows/deploy.yml."
            exit 1
          fi
          if [ "${status}" != "completed" ] || [ "${conclusion}" != "success" ]; then
            echo "::error::Platform deploy run ${PLATFORM_DEPLOY_RUN_ID} has status='${status}' conclusion='${conclusion}'; promotion requires a fully green deploy run."
            exit 1
          fi
          rm -rf platform-proof
          mkdir platform-proof
          gh run download "${PLATFORM_DEPLOY_RUN_ID}" \
            --repo aexhq/aex-platform \
            --name "promotion-proof-${PLATFORM_DEPLOY_RUN_ID}" \
            --dir platform-proof
          proof_file="$(find platform-proof -name promotion-proof.json -print -quit)"
          if [ -z "${proof_file}" ]; then
            echo "::error::Platform deploy run ${PLATFORM_DEPLOY_RUN_ID} did not include promotion-proof.json."
            exit 1
          fi
          proof_workflow="$(jq -r '.workflow // empty' "${proof_file}")"
          proof_run_id="$(jq -r '.runId // empty' "${proof_file}")"
          proof_version="$(jq -r '.sdkVersion // empty' "${proof_file}")"
          if [ "${proof_workflow}" != "deploy.yml" ] || [ "${proof_run_id}" != "${PLATFORM_DEPLOY_RUN_ID}" ]; then
            echo "::error::Platform promotion proof does not match deploy run ${PLATFORM_DEPLOY_RUN_ID}."
            exit 1
          fi
          if [ "${proof_version}" != "${PACKAGE_VERSION}" ]; then
            echo "::error::Platform deploy proof tested sdkVersion='${proof_version}', not '${PACKAGE_VERSION}'."
            exit 1
          fi
          for gate in suite_dev spot_canary_dev suite_prod smoke_prod; do
            if ! jq -e --arg gate "${gate}" '.gates | index($gate)' "${proof_file}" >/dev/null; then
              echo "::error::Platform promotion proof is missing gate '${gate}'."
              exit 1
            fi
          done
          echo "Verified: green aex-platform deploy run ${PLATFORM_DEPLOY_RUN_ID} tested @aexhq/sdk@${PACKAGE_VERSION}."

      - name: Verify platform validation manifest
        env:
          GH_TOKEN: ${{ secrets.PLATFORM_REPO_TOKEN }}
          PLATFORM_DEPLOY_RUN_ID: ${{ inputs.platform_deploy_run_id }}
          RELEASE_RUN_ID: ${{ inputs.release_run_id }}
          PACKAGE_VERSION: ${{ inputs.version }}
        shell: bash
        run: |
          set -euo pipefail
          rm -rf platform-validation-manifest
          mkdir platform-validation-manifest
          gh run download "${PLATFORM_DEPLOY_RUN_ID}" \
            --repo aexhq/aex-platform \
            --name "platform-validation-manifest-${PLATFORM_DEPLOY_RUN_ID}" \
            --dir platform-validation-manifest
          manifest_file="$(find platform-validation-manifest -name platform-validation-manifest.json -print -quit)"
          if [ -z "${manifest_file}" ]; then
            echo "::error::Platform deploy run ${PLATFORM_DEPLOY_RUN_ID} did not include platform-validation-manifest.json."
            exit 1
          fi
          bun scripts/cicd/release-manifest.mjs verify-platform \
            --manifest "${manifest_file}" \
            --version "${PACKAGE_VERSION}" \
            --run-id "${PLATFORM_DEPLOY_RUN_ID}" \
            --public-release-run-id "${RELEASE_RUN_ID}"

      - name: Assert version exists on npm
        run: node scripts/cicd/wait-for-npm.mjs @aexhq/sdk "${{ inputs.version }}"

      - name: Assert version installs with Bun
        run: bun scripts/cicd/wait-for-bun-install.mjs @aexhq/sdk "${{ inputs.version }}"

      - name: Add npm dist-tag
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_DIST_TAG: ${{ inputs.npm_dist_tag }}
        shell: bash
        run: |
          set -euo pipefail
          if [ -z "${NODE_AUTH_TOKEN:-}" ]; then
            echo "NPM_TOKEN is required in the npm-release environment for npm dist-tag promotion." >&2
            exit 1
          fi
          printf '//registry.npmjs.org/:_authToken=%s\n' "${NODE_AUTH_TOKEN}" > "$HOME/.npmrc"
          npm dist-tag add "@aexhq/sdk@${{ inputs.version }}" "${NPM_DIST_TAG}" --registry https://registry.npmjs.org
          npm dist-tag ls @aexhq/sdk --registry https://registry.npmjs.org
