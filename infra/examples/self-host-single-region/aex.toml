[aex]
owner = "delivery"
role = "composition"
artifact = "none"
layers = ["unit", "integration"]
concerns = ["security"]
seams = []
security_tier = "internal"
risk = ["none"]
scenarios = []

[aex.not_applicable]
live_suite = "A self-hoster applies this root against their own account; this repository never does, and cannot own evidence for someone else's plane."
