[aex]
owner = "delivery"
role = "infra_module"
artifact = "terraform_module"
layers = ["unit", "integration"]
concerns = ["security"]
seams = []
security_tier = "internal"
risk = ["iam"]
scenarios = []

[aex.not_applicable]
live_suite = "Key policy and rotation are declarative; the encryption-context denial they express is proven by the aws.kms.encryption_context seam owned by the regional-stores live suite, not by this module."
