[aex]
owner = "delivery"
role = "infra_module"
artifact = "terraform_module"
layers = ["unit", "integration"]
concerns = ["security"]
seams = []
security_tier = "internal"
risk = ["none"]
scenarios = []

[aex.not_applicable]
live_suite = "Alarm evaluation depends on metrics only a deployed product emits; the emitting deployable's live suite owns that proof."
