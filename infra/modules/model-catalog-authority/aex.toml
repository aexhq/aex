[aex]
owner = "delivery"
role = "infra_module"
artifact = "terraform_module"
layers = ["unit", "integration"]
concerns = ["security"]
seams = []
security_tier = "internal"
risk = ["iam"]
scenarios = []

[aex.not_applicable]
live_suite = "The dedicated signing key and OIDC trust are exercised only by the protected publication workflow; this module has no deployed product endpoint to probe."
