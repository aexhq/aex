[aex]
owner = "delivery"
role = "infra_module"
artifact = "terraform_module"
layers = ["unit", "integration"]
concerns = ["security"]
seams = []
security_tier = "public_edge"
risk = ["iam", "untrusted_input"]
scenarios = []

[aex.not_applicable]
live_suite = "Route, authorizer and alias integration behaviour is proven by each mounted central service's public-edge live suite."
