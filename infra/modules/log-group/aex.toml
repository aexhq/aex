[aex]
owner = "delivery"
role = "infra_module"
artifact = "terraform_module"
layers = ["unit", "integration"]
concerns = ["security"]
seams = []
security_tier = "internal"
risk = ["none"]
scenarios = []

[aex.not_applicable]
live_suite = "Delivery and retention of a log line are AWS behaviour; the deployables' own live suites read their groups and are what would notice a group that accepts nothing."
