[aex]
owner = "delivery"
role = "infra_module"
artifact = "terraform_module"
layers = ["unit", "integration"]
concerns = ["security"]
seams = []
security_tier = "internal"
risk = ["none"]
scenarios = []

[aex.not_applicable]
live_suite = "Table shape is declarative; stream, TTL and throttling behaviour belong to the aws.dynamodb.* seams owned by the regional-stores live suite."
