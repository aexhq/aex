[aex]
owner = "delivery"
role = "infra_module"
artifact = "terraform_module"
layers = ["unit", "integration"]
concerns = ["security"]
seams = []
security_tier = "internal"
risk = ["none"]
scenarios = []

[aex.not_applicable]
live_suite = "The function resource has no behaviour of its own; the deployed code is proven by the owning deployable's live suite."
