[aex]
owner = "delivery"
role = "infra_module"
artifact = "terraform_module"
layers = ["unit", "integration"]
concerns = ["security"]
seams = []
security_tier = "internal"
risk = ["iam"]
scenarios = []

[aex.not_applicable]
live_suite = "Role documents are declarative; the allow/deny matrix is proven by the aws.iam.denial seam owned by each deployable's own live suite."
