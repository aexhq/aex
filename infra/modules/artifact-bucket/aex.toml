[aex]
owner = "delivery"
role = "infra_module"
artifact = "terraform_module"
layers = ["unit", "integration"]
concerns = ["security"]
seams = []
security_tier = "internal"
risk = ["none"]
scenarios = []

[aex.not_applicable]
live_suite = "A versioned infrastructure-artifact bucket has no product runtime path; retention is proven by lifecycle configuration review, not by a live suite."
