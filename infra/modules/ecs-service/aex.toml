[aex]
owner = "delivery"
role = "infra_module"
artifact = "terraform_module"
layers = ["unit", "integration"]
concerns = ["security"]
seams = []
security_tier = "internal"
risk = ["none"]
scenarios = []

[aex.not_applicable]
live_suite = "Service drain, deregistration and scaling are proven by the aws.ecs.run_task seam owned by the brain-mux and session-api live suites."
