# `infra/modules/`

Public reusable Terraform modules: one module per authority boundary, each with
its own variables, outputs, policy tests and least-privilege denial tests.

Owner: the infrastructure stream.
