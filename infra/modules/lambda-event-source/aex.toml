[aex]
owner = "delivery"
role = "infra_module"
artifact = "terraform_module"
layers = ["unit", "integration"]
concerns = ["security"]
seams = []
security_tier = "internal"
risk = ["none"]
scenarios = []

[aex.not_applicable]
live_suite = "Partial-batch and redrive behaviour is proven by the aws.sqs.fifo and aws.dynamodb.streams seams owned by the consuming deployable's live suite."
