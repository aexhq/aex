[aex]
owner = "delivery"
role = "infra_module"
artifact = "terraform_module"
layers = ["unit", "integration"]
concerns = ["security"]
seams = []
security_tier = "internal"
risk = ["none"]
scenarios = []

[aex.not_applicable]
live_suite = "Whether a flood is actually blocked is a property of the deployed web ACL, proven by the public-edge live suite of the service behind the load balancer; a plan can only prove the rule is the one that was asked for."
