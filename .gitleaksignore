# Reviewed gitleaks findings for this repository.
#
# One fingerprint per line, its reason on the comment line(s) directly above, no
# bare suppressions, no stale entries. The private platform repository holds an
# allowlist of its own under the identical rules, and its `secret_scan` CI job
# scans a checkout of THIS repository against THIS file — the gate refuses a
# finding absent here, an entry with no stated reason, and an entry no finding
# matches any more.
#
# Current strict-v1 scan measured 2026-07-30 (gitleaks 8.30.1). The remaining
# findings are deliberate redaction-corpus inputs:

# Synthetic fixture: a jwt.io-shaped HS256 token in the SDK secret-redaction
# corpus. It carries no production signing material and validates nowhere.
packages/contracts/test/sdk-secrets.test.ts:jwt:23

# Synthetic fixture: a fake Slack bot token in the same redaction corpus.
packages/contracts/test/sdk-secrets.test.ts:generic-api-key:26
