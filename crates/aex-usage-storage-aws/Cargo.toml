[package]
name = "aex-usage-storage-aws"
version = "0.1.0"
description = "`aex-usage-storage-aws` is the `usage-storage-authority` adapter: table expressions, the row codec, the admission transaction and the frontier compare-and-set for `storage.byte_min.v1` facts, plus the rating-queue producer and this category's settlement receipt consumer."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[package.metadata.aex]
owner = "observations-usage"
role = "adapter"
artifact = "none"
live_suite = "aex-live-usage-storage-worker"
layers = ["unit", "integration"]
concerns = ["contract", "property", "fault", "security"]
seams = ["aws.dynamodb.condition", "aws.s3.conditional_put"]
security_tier = "authority"
risk = ["concurrency"]
scenarios = []


[package.metadata.aex.not_applicable]
integration = "DEFERRED, not inapplicable: the DynamoDB Local suite (concurrent producers converging on a contiguous sequence, ambiguous-commit resolution by re-reading ID#, replay vs conflict discrimination) is unwritten. The expression and codec halves are covered at the unit layer; the transaction's live behaviour is not."

[package.metadata.aex.targets]
isolation = "unit"

[dependencies]
async-trait.workspace = true
aws-config.workspace = true
aws-sdk-dynamodb.workspace = true
aws-sdk-sqs.workspace = true
aws-smithy-types.workspace = true
futures.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
time.workspace = true
uuid.workspace = true
xxhash-rust.workspace = true
aex-usage-app = { path = "../aex-usage-app" }
aex-usage-domain = { path = "../aex-usage-domain" }

[dev-dependencies]
aws-smithy-runtime-api.workspace = true
tokio = { workspace = true, features = ["rt", "macros"] }
