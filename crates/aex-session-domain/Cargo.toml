[package]
name = "aex-session-domain"
version = "0.1.0"
description = "`aex-session-domain` owns the pure session, run, agent and journal model: fold order, lineage, the terminal barrier and idempotency."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[package.metadata.aex]
owner = "regional-domains"
role = "domain"
artifact = "none"
layers = ["unit"]
concerns = ["property"]
seams = []
security_tier = "authority"
risk = ["concurrency"]
scenarios = []

[package.metadata.aex.targets]
properties = "unit"

[package.metadata.aex.not_applicable]
live_suite = "pure domain crate; Area 9's registry declares no independent live seam"

[dependencies]
aex-content-domain = { path = "../aex-content-domain" }
aex-internal-contracts = { path = "../aex-internal-contracts" }
aex-operation-domain = { path = "../aex-operation-domain" }
aex-secret-domain = { path = "../aex-secret-domain" }
aex-wire = { path = "../aex-wire" }
sha2.workspace = true
serde_json.workspace = true
thiserror.workspace = true
time.workspace = true

[dev-dependencies]
proptest.workspace = true
