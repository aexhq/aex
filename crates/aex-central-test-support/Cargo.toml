[package]
name = "aex-central-test-support"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[package.metadata.aex]
owner = "central-identity"
role = "test_support"
artifact = "none"
layers = ["unit"]
concerns = ["property"]
seams = []
security_tier = "diagnostic"
risk = ["none"]
scenarios = []

[package.metadata.aex.not_applicable]
targets = "awaiting the central-identity stream; the cases land with the crate's own implementation"
live_suite = "test-only crate; must be absent from every production link graph"

[dependencies]
aex-test-harness = { path = "../../tests/support/aex-test-harness" }
thiserror.workspace = true
time.workspace = true
uuid.workspace = true
