[package]
name = "aex-usage-rating"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[package.metadata.aex]
owner = "central-finance"
role = "domain"
artifact = "none"
layers = ["unit"]
concerns = ["property"]
seams = []
security_tier = "money"
risk = ["money"]
scenarios = []

[package.metadata.aex.not_applicable]
targets = "awaiting the central-finance stream; the cases land with the crate's own implementation"
live_suite = "pure domain crate; Area 9's registry declares no independent live seam"

[dependencies]
num-bigint.workspace = true
num-rational.workspace = true
num-traits.workspace = true
serde.workspace = true
thiserror.workspace = true
