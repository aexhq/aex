[package]
name = "aex-usage-rating"
version = "0.1.0"
description = "`aex-usage-rating` owns the pure exact rating policy: the immutable rate-card schema, exact rational arithmetic and deterministic settlement rounding."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[package.metadata.aex]
owner = "central-finance"
role = "domain"
artifact = "none"
layers = ["unit"]
concerns = ["property"]
seams = []
security_tier = "money"
risk = ["money"]
scenarios = []

[package.metadata.aex.not_applicable]
live_suite = "pure domain crate; Area 9's registry declares no independent live seam"

[package.metadata.aex.targets]
properties = "unit"

[dependencies]
aex-finance-domain = { path = "../aex-finance-domain" }
aex-internal-contracts = { path = "../aex-internal-contracts" }
aex-wire = { path = "../aex-wire" }
blake3.workspace = true
hex.workspace = true
num-bigint.workspace = true
num-rational.workspace = true
num-traits.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
thiserror.workspace = true
time.workspace = true

[dev-dependencies]
insta.workspace = true
proptest.workspace = true
