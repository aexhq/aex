[package]
name = "aex-secret-domain"
version = "0.1.0"
description = "`aex-secret-domain` owns the pure secret custody, generation, rotation and revocation model."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[package.metadata.aex]
owner = "regional-domains"
role = "domain"
artifact = "none"
layers = ["unit"]
concerns = ["property", "security"]
seams = []
security_tier = "secret"
risk = ["none"]
scenarios = []

[package.metadata.aex.targets]
properties = "unit"
security = "unit"

[package.metadata.aex.not_applicable]
live_suite = "pure domain crate; Area 9's registry declares no independent live seam"

[dependencies]
aex-wire = { path = "../aex-wire" }
blake3.workspace = true
sha2.workspace = true
thiserror.workspace = true
zeroize.workspace = true

[dev-dependencies]
proptest.workspace = true
sha2.workspace = true
