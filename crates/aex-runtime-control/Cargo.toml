[package]
name = "aex-runtime-control"
version = "0.1.0"
description = "`aex-runtime-control` owns the pure Hands lifecycle and true-idle model, including the exact 179999/180000 millisecond idle boundary and generation fencing."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[package.metadata.aex]
owner = "hands"
role = "domain"
artifact = "none"
layers = ["unit"]
concerns = ["property"]
seams = []
security_tier = "internal"
risk = ["concurrency"]
scenarios = []

[package.metadata.aex.not_applicable]
targets = "the `hands` stream places this crate's evidence in inline unit modules, which the declared `unit` layer already collects; the crate declares no [[test]] target, so there is nothing to map"
live_suite = "pure domain crate; Area 9's registry declares no independent live seam"

[dependencies]
aex-hands-protocol = { path = "../aex-hands-protocol" }
aex-internal-contracts = { path = "../aex-internal-contracts" }
aex-usage-domain = { path = "../aex-usage-domain" }
aex-wire = { path = "../aex-wire" }
serde.workspace = true
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true
serde_json.workspace = true
