[package]
name = "aex-hands-protocol"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[package.metadata.aex]
owner = "contracts"
role = "contract"
artifact = "none"
layers = ["unit"]
concerns = ["contract", "property", "security"]
seams = []
security_tier = "guest"
risk = ["untrusted_input", "generated_code"]
scenarios = []

[package.metadata.aex.not_applicable]
targets = "awaiting the contracts stream; the cases land with the crate's own implementation"
live_suite = "generated contract crate; its wire is exercised through every deployable's companion, not a live seam of its own"

[dependencies]
bytes.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
time.workspace = true
uuid.workspace = true
