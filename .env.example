# Copy to .env.local for local user-test runs.
AEX_API_URL=
AEX_API_TOKEN=
ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=
# Doubao (ByteDance) via the official Ark API. Optional — the live Doubao
# scenario self-skips when this is unset.
DOUBAO_API_KEY=
AEX_USER_TEST_TARBALL=
AEX_USER_TEST_VERSION=
