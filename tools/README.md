# `tools/`

Workspace tooling. `aex-cli` is the supported native CLI, `aex-release-tool`
owns release graph and evidence mechanics, `aex-contract-gen` generates the
contract bundle, and `aex-workspace-check` enforces the workspace's structural
rules.

`eslint-plugin-aex` is a retained TypeScript package and is not a Cargo member.
