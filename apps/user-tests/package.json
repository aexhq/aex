{
  "name": "@aexhq/user-tests",
  "version": "0.50.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "description": "Layer-4 'user tests' — exercise the packed/published aex artifact in a clean install tree, the way a real user or AI agent would on day one. Product SDK/CLI access goes through a child process whose cwd is a fresh `bun install` tempdir; the harness may import local test fixtures.",
  "aex": {
    "owner": "clients",
    "role": "test_support",
    "artifact": "none",
    "layers": [
      "unit",
      "user"
    ],
    "concerns": [
      "property",
      "compatibility"
    ],
    "seams": [],
    "security_tier": "public_edge",
    "risk": [
      "generated_code"
    ],
    "scenarios": ["packed.sdk-install-node", "local.site-determinism", "live.session-round-trip"],
    "targets": {
      "registry": "unit",
      "public_user": "user"
    },
    "not_applicable": {
      "live_suite": "the public user-test authority observes deployed planes through published artifacts only; it owns no companion"
    }
  },
  "scripts": {
    "test:user": "bun test --isolate test/packed test/local test/browser test/money test/operator test/registry.test.ts",
    "test:user:files": "bun test --isolate test/packed test/local test/browser test/money test/operator test/registry.test.ts",
    "test:user:live": "bun test --isolate test/live",
    "test:user:smoke": "bun test --isolate test/registry.test.ts",
    "test:user:offline": "bun test --isolate test/packed test/local test/registry.test.ts",
    "test:unit": "bun test --isolate test/packed test/local test/browser test/money test/operator test/registry.test.ts",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "@aexhq/sdk": "workspace:*",
    "typescript": "5.8.3"
  }
}
