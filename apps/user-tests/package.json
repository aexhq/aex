{
  "name": "@antpath/user-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Layer-4 'user tests' — exercise the packed/published antpath artifact in a clean install tree, the way a real user or AI agent would on day one. NEVER imports antpath or @antpath/* workspace packages directly; everything goes through a child process whose cwd is a fresh `npm install` tempdir.",
  "scripts": {
    "test:user": "vitest run --config vitest.config.ts test",
    "test:user:offline": "vitest run --config vitest.config.ts --maxWorkers 1 --minWorkers 1 test/offline",
    "test:user:heavy": "vitest run --config vitest.heavy.config.ts",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@antpath/conformance": "workspace:*",
    "dotenv": "16.5.0",
    "typescript": "5.8.3",
    "vitest": "3.1.2"
  }
}
