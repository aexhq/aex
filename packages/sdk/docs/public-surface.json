{
  "productName": "aex",
  "oneLine": "Explicit durable agent sessions through the strict aex v1 API.",
  "description": "Create sessions, admit messages and runs, manage overwrite-by-name workspace resources, read persisted or live files through grants, and query or export telemetry.",
  "examples": {
    "typescriptLines": [
      "import { Aex } from \"@aexhq/sdk\";",
      "",
      "const aex = new Aex(process.env.AEX_WORKSPACE_API_KEY!);",
      "const session = await aex.sessions.create({",
      "  model: \"anthropic/claude-haiku-4-5\"",
      "});",
      "const { run } = await session.messages.send(\"Summarize this repository.\");",
      "const result = await run.result();",
      "console.log(result.id, result.status);"
    ],
    "cliLines": [
      "bun add --global @aexhq/cli",
      "aex sessions create --request @session.json --api-key \"$AEX_WORKSPACE_API_KEY\""
    ]
  },
  "featureAreas": [
    {
      "title": "Sessions and runs",
      "href": "/docs/concepts/sessions/",
      "description": "Explicit session creation, message admission, durable run polling, and operation-backed lifecycle changes."
    },
    {
      "title": "Registered resources and files",
      "href": "/docs/guides/resources/",
      "description": "Overwrite-by-name inputs plus distinct persisted and live file reads with short-lived download grants."
    },
    {
      "title": "Telemetry",
      "href": "/docs/guides/telemetry/",
      "description": "Query, stream, aggregate, inspect gaps, and create durable telemetry exports."
    },
    {
      "title": "Account and billing",
      "href": "/docs/guides/billing/",
      "description": "Bootstrap resources for accounts, organizations, workspaces, API keys, balances, usage, and statements."
    }
  ]
}
