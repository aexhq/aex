{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // bun:test types replace the root vitest/globals injection for this
    // bun-test-native package (explicit imports only; no test globals).
    "types": ["node", "bun"]
  },
  "include": ["src", "test", "scripts"]
}
