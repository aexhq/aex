{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Explicit per-package pin of the shared test-runner types (matches
    // tsconfig.base.json; dedup is a later cleanup).
    "types": ["node", "bun"]
  },
  "include": ["src", "test", "scripts"]
}
