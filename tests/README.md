# `tests/`

`live/` holds one `publish = false` companion package per deployable, selected
only by the live lane. `load/` holds capacity, pressure and soak workloads.

Neither directory is part of the default test lane.
