[package]
name = "aex-live-observation-reconciler"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[package.metadata.aex]
owner = "observations-usage"
role = "live_companion"
artifact = "none"
deployable = "observation-reconciler"
layers = ["smoke", "e2e"]
concerns = ["fault", "security", "performance"]
seams = ["aws.dynamodb.streams", "aws.dynamodb.ttl", "aws.iam.denial", "aws.s3.conditional_put"]
security_tier = "internal"
risk = ["iam"]
scenarios = []

[package.metadata.aex.not_applicable]
targets = "awaiting the observations-usage stream; live evidence cannot be earned before deployment (OD-07)"
