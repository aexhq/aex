[package]
name = "aex-live-regional-capacity-controller"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[package.metadata.aex]
owner = "regional-services"
role = "live_companion"
artifact = "none"
deployable = "regional-capacity-controller"
layers = ["smoke", "e2e"]
concerns = ["fault", "security", "performance"]
seams = ["aws.dynamodb.transact_write", "aws.iam.denial"]
security_tier = "authority"
risk = ["iam", "concurrency"]
scenarios = []

[package.metadata.aex.not_applicable]
targets = "live evidence requires the private regional deployment composition"
