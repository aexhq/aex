[package]
name = "aex-live-usage-receipt-dispatcher"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[package.metadata.aex]
owner = "central-finance"
role = "live_companion"
artifact = "none"
deployable = "usage-receipt-dispatcher"
layers = ["smoke", "e2e"]
concerns = ["fault", "security", "performance"]
seams = ["aws.dynamodb.streams", "aws.iam.denial", "aws.rds_data.transaction", "aws.sqs.fifo", "aws.sqs.redrive"]
security_tier = "money"
risk = ["iam"]
scenarios = []

[package.metadata.aex.not_applicable]
targets = "awaiting the central-finance stream; live evidence cannot be earned before deployment (OD-07)"
