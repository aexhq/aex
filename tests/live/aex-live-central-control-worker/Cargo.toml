[package]
name = "aex-live-central-control-worker"
version = "0.1.0"
description = "Live-test companion package for the `central-control-worker` deployable."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[package.metadata.aex]
owner = "central-identity"
role = "live_companion"
artifact = "none"
deployable = "central-control-worker"
layers = ["smoke", "e2e"]
concerns = ["fault", "security", "performance"]
seams = ["aws.iam.denial", "aws.lambda.async_delivery", "aws.rds_data.transaction"]
security_tier = "internal"
risk = ["iam"]
scenarios = []

[package.metadata.aex.targets]
live = "e2e"
