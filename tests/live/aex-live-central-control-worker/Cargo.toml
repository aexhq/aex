[package]
name = "aex-live-central-control-worker"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[package.metadata.aex]
owner = "central-identity"
role = "live_companion"
artifact = "none"
deployable = "central-control-worker"
layers = ["smoke", "e2e"]
concerns = ["fault", "security", "performance"]
seams = ["aws.iam.denial", "aws.rds_data.transaction", "aws.sqs.fifo"]
security_tier = "internal"
risk = ["iam"]
scenarios = []

[package.metadata.aex.not_applicable]
targets = "awaiting the central-identity stream; live evidence cannot be earned before deployment (OD-07)"
