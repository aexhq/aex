[package]
name = "aex-live-central-schema-admin"
version = "0.1.0"
description = "Live-test companion package for the `central-schema-admin` deployable."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[package.metadata.aex]
owner = "central-finance"
role = "live_companion"
artifact = "none"
deployable = "central-schema-admin"
layers = ["smoke", "e2e"]
concerns = ["fault", "security", "performance"]
seams = ["aws.aurora.serverless_v2", "aws.ecs.run_task", "aws.iam.denial"]
security_tier = "internal"
risk = ["iam"]
scenarios = []

[package.metadata.aex.not_applicable]
targets = "awaiting the central-finance stream; live evidence cannot be earned before deployment (OD-07)"
