[package]
name = "aex-live-regional-observation-api"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[package.metadata.aex]
owner = "observations-usage"
role = "live_companion"
artifact = "none"
deployable = "regional-observation-api"
layers = ["smoke", "e2e"]
concerns = ["fault", "security", "performance"]
seams = ["aws.dynamodb.condition", "aws.dynamodb.streams", "aws.dynamodb.transact_write", "aws.iam.denial", "aws.s3.conditional_put"]
security_tier = "public_edge"
risk = ["iam"]
scenarios = []

[package.metadata.aex.not_applicable]
targets = "awaiting the observations-usage stream; live evidence cannot be earned before deployment (OD-07)"
