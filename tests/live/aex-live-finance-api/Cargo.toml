[package]
name = "aex-live-finance-api"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[package.metadata.aex]
owner = "central-finance"
role = "live_companion"
artifact = "none"
deployable = "finance-api"
layers = ["smoke", "e2e"]
concerns = ["fault", "security", "performance"]
seams = ["aws.aurora.serverless_v2", "aws.iam.denial", "aws.rds_data.transaction", "stripe.test_mode.webhook"]
security_tier = "public_edge"
risk = ["iam"]
scenarios = []

[package.metadata.aex.not_applicable]
targets = "awaiting the central-finance stream; live evidence cannot be earned before deployment (OD-07)"
