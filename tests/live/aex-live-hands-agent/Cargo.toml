[package]
name = "aex-live-hands-agent"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[package.metadata.aex]
owner = "hands"
role = "live_companion"
artifact = "none"
deployable = "hands-agent"
layers = ["smoke", "e2e"]
concerns = ["fault", "security", "performance"]
seams = ["hands.guest.lifecycle"]
security_tier = "guest"
risk = ["iam"]
scenarios = []

[package.metadata.aex.not_applicable]
targets = "awaiting the hands stream; live evidence cannot be earned before deployment (OD-07)"
