[package]
name = "aex-live-hands-agent"
version = "0.1.0"
description = "Live-test companion package for the `hands-agent` deployable."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[package.metadata.aex]
owner = "hands"
role = "live_companion"
artifact = "none"
deployable = "hands-agent"
layers = ["smoke", "e2e"]
concerns = ["fault", "security", "performance"]
seams = ["hands.guest.lifecycle"]
security_tier = "guest"
risk = ["iam"]
scenarios = []

[package.metadata.aex.targets]
guest = "e2e"
