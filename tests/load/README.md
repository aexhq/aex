# `tests/load/`

Capacity, pressure, scale and soak workloads. They are selected explicitly by
the load lane and report cost per completed turn, per API operation, per export
and per observation batch alongside their latency results.

Owner: the test-architecture stream.
