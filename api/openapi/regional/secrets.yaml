plane: regional
fragment: secrets
operations:
  - { id: secrets_list, method: GET, path: /api/workspace/secrets, summary: "List secret metadata; values are never readable.", scope: "secrets:read", success: SecretMetadataPage, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, invalid_cursor], query: { cursor: { type: cursor, optional: true, doc: "Opaque continuation token." }, limit: { type: integer, min: 1, max: 1000, optional: true, doc: "Page size." } } }
  - { id: secret_get, method: GET, path: "/api/workspace/secrets/{name}", summary: "Read one secret metadata record.", scope: "secrets:read", etag: returns, success: SecretMetadata, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found] }
  - { id: secret_put, method: PUT, path: "/api/workspace/secrets/{name}", summary: "Set a secret value for future admission.", scope: "secrets:write", idempotency: idempotency_key, etag: optional_if_match, request: SecretPutRequest, success: SecretMetadata, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, invalid_request, idempotency_conflict, precondition_failed, limit_exceeded] }
  - { id: secret_delete, method: DELETE, path: "/api/workspace/secrets/{name}", summary: "Delete a secret, affecting future admission only.", scope: "secrets:write", etag: optional_if_match, pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, precondition_failed] }
  - { id: secret_revoke, method: POST, path: "/api/workspace/secrets/{name}/revocations", summary: "Revoke a secret and cancel current custody.", scope: "secrets:revoke", idempotency: idempotency_key, request: EmptyRequest, success: SecretRevocation, pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, not_found, idempotency_conflict] }
