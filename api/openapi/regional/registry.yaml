# Overwrite-only registered resources.
#
# The revision and the ETag identify the current replacement for concurrency
# control only. No old value is addressable and none is promised to remain
# retained after replacement.
plane: regional
fragment: registry
operations:
  - { id: registry_files_list, method: GET, path: /api/workspace/files, summary: "List registered files.", scope: "resources:read", success: RegisteredFilePage, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, invalid_cursor], query: { cursor: { type: cursor, optional: true, doc: "Opaque continuation token." }, limit: { type: integer, min: 1, max: 1000, optional: true, doc: "Page size." } } }
  - { id: registry_files_get, method: GET, path: "/api/workspace/files/{name}", summary: "Read one registered entry from files.", scope: "resources:read", etag: returns, success: RegisteredFile, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found] }
  - { id: registry_files_put, method: PUT, path: "/api/workspace/files/{name}", summary: "Replace one registered entry in files.", scope: "resources:write", idempotency: idempotency_key, etag: optional_if_match, request: RegisteredFileValue, success: RegisteredFile, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, invalid_request, idempotency_conflict, precondition_failed, limit_exceeded, content_missing] }
  - { id: registry_files_delete, method: DELETE, path: "/api/workspace/files/{name}", summary: "Delete one registered entry from files.", scope: "resources:write", etag: optional_if_match, pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, precondition_failed] }
  - { id: registry_skills_list, method: GET, path: /api/workspace/skills, summary: "List registered skills.", scope: "resources:read", success: RegisteredSkillPage, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, invalid_cursor], query: { cursor: { type: cursor, optional: true, doc: "Opaque continuation token." }, limit: { type: integer, min: 1, max: 1000, optional: true, doc: "Page size." } } }
  - { id: registry_skills_get, method: GET, path: "/api/workspace/skills/{name}", summary: "Read one registered entry from skills.", scope: "resources:read", etag: returns, success: RegisteredSkill, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found] }
  - { id: registry_skills_put, method: PUT, path: "/api/workspace/skills/{name}", summary: "Replace one registered entry in skills.", scope: "resources:write", idempotency: idempotency_key, etag: optional_if_match, request: RegisteredSkillValue, success: RegisteredSkill, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, invalid_request, idempotency_conflict, precondition_failed, limit_exceeded, content_missing] }
  - { id: registry_skills_delete, method: DELETE, path: "/api/workspace/skills/{name}", summary: "Delete one registered entry from skills.", scope: "resources:write", etag: optional_if_match, pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, precondition_failed] }
  - { id: registry_tools_list, method: GET, path: /api/workspace/tools, summary: "List registered tools.", scope: "resources:read", success: RegisteredToolPage, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, invalid_cursor], query: { cursor: { type: cursor, optional: true, doc: "Opaque continuation token." }, limit: { type: integer, min: 1, max: 1000, optional: true, doc: "Page size." } } }
  - { id: registry_tools_get, method: GET, path: "/api/workspace/tools/{name}", summary: "Read one registered entry from tools.", scope: "resources:read", etag: returns, success: RegisteredTool, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found] }
  - { id: registry_tools_put, method: PUT, path: "/api/workspace/tools/{name}", summary: "Replace one registered entry in tools.", scope: "resources:write", idempotency: idempotency_key, etag: optional_if_match, request: RegisteredToolValue, success: RegisteredTool, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, invalid_request, idempotency_conflict, precondition_failed, limit_exceeded, content_missing] }
  - { id: registry_tools_delete, method: DELETE, path: "/api/workspace/tools/{name}", summary: "Delete one registered entry from tools.", scope: "resources:write", etag: optional_if_match, pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, precondition_failed] }
  - { id: registry_instructions_list, method: GET, path: /api/workspace/instructions, summary: "List registered instructions.", scope: "resources:read", success: RegisteredInstructionPage, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, invalid_cursor], query: { cursor: { type: cursor, optional: true, doc: "Opaque continuation token." }, limit: { type: integer, min: 1, max: 1000, optional: true, doc: "Page size." } } }
  - { id: registry_instructions_get, method: GET, path: "/api/workspace/instructions/{name}", summary: "Read one registered entry from instructions.", scope: "resources:read", etag: returns, success: RegisteredInstruction, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found] }
  - { id: registry_instructions_put, method: PUT, path: "/api/workspace/instructions/{name}", summary: "Replace one registered entry in instructions.", scope: "resources:write", idempotency: idempotency_key, etag: optional_if_match, request: RegisteredInstructionValue, success: RegisteredInstruction, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, invalid_request, idempotency_conflict, precondition_failed, limit_exceeded, content_missing] }
  - { id: registry_instructions_delete, method: DELETE, path: "/api/workspace/instructions/{name}", summary: "Delete one registered entry from instructions.", scope: "resources:write", etag: optional_if_match, pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, precondition_failed] }
  - { id: registry_mcp_servers_list, method: GET, path: /api/workspace/mcp-servers, summary: "List registered MCP servers.", scope: "resources:read", success: RegisteredMcpServerPage, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, invalid_cursor], query: { cursor: { type: cursor, optional: true, doc: "Opaque continuation token." }, limit: { type: integer, min: 1, max: 1000, optional: true, doc: "Page size." } } }
  - { id: registry_mcp_servers_get, method: GET, path: "/api/workspace/mcp-servers/{name}", summary: "Read one registered entry from MCP servers.", scope: "resources:read", etag: returns, success: RegisteredMcpServer, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found] }
  - { id: registry_mcp_servers_put, method: PUT, path: "/api/workspace/mcp-servers/{name}", summary: "Replace one registered entry in MCP servers.", scope: "resources:write", idempotency: idempotency_key, etag: optional_if_match, request: RegisteredMcpServerValue, success: RegisteredMcpServer, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, invalid_request, idempotency_conflict, precondition_failed, limit_exceeded, content_missing] }
  - { id: registry_mcp_servers_delete, method: DELETE, path: "/api/workspace/mcp-servers/{name}", summary: "Delete one registered entry from MCP servers.", scope: "resources:write", etag: optional_if_match, pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, precondition_failed] }
  - { id: registry_files_download_create, method: POST, path: "/api/workspace/files/{name}/downloads", summary: "Mint a download grant for a registered file.", scope: "resources:read", idempotency: idempotency_key, request: RegistryDownloadRequest, successStatus: 201, success: DownloadGrant, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, invalid_request, invalid_range, idempotency_conflict, content_missing] }
