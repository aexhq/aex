plane: regional
fragment: files
operations:
  - { id: session_files_persisted_list, method: POST, path: "/api/sessions/{sessionId}/files/persisted/list", summary: "List persisted session files; an observational read.", scope: "files:read", request: FileListRequest, success: FileEntryPage, safeRetry: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleted, invalid_request, invalid_cursor] }
  - { id: session_files_persisted_stat, method: POST, path: "/api/sessions/{sessionId}/files/persisted/stat", summary: "Stat one persisted session file.", scope: "files:read", request: FileStatRequest, success: FileEntry, safeRetry: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleted, invalid_request, file_not_found] }
  - { id: session_files_persisted_download_create, method: POST, path: "/api/sessions/{sessionId}/files/persisted/downloads", summary: "Mint a download grant for a persisted session file.", scope: "files:read", idempotency: idempotency_key, request: FileDownloadRequest, successStatus: 201, success: DownloadGrant, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleted, invalid_request, invalid_range, file_not_found, content_missing, idempotency_conflict] }
  - { id: session_files_live_list, method: POST, path: "/api/sessions/{sessionId}/files/live/list", summary: "List live workspace files, optionally waking a retained session.", scope: "files:live", request: LiveFileListRequest, success: LiveFileEntryPage, safeRetry: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleting, session_deleted, invalid_request, invalid_cursor, precondition_failed, workspace_activation_required] }
  - { id: session_files_live_stat, method: POST, path: "/api/sessions/{sessionId}/files/live/stat", summary: "Stat one live workspace file.", scope: "files:live", request: LiveFileStatRequest, success: LiveFileEntry, safeRetry: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleting, session_deleted, invalid_request, file_not_found, precondition_failed, workspace_activation_required] }
  - { id: session_files_live_download_create, method: POST, path: "/api/sessions/{sessionId}/files/live/downloads", summary: "Mint a download grant for a live workspace file.", scope: "files:live", idempotency: idempotency_key, request: LiveFileDownloadRequest, successStatus: 201, success: LiveDownloadGrant, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleting, session_deleted, invalid_request, invalid_range, file_not_found, content_missing, idempotency_conflict, precondition_failed, workspace_activation_required] }
