plane: regional
fragment: files
operations:
  - { id: session_files_live_list, method: POST, path: "/api/sessions/{sessionId}/files/live/list", summary: "List the exact live generation, auto-resuming that same generation when suspended.", scope: "files:live", request: LiveFileListRequest, success: LiveFileEntryPage, safeRetry: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleting, session_deleted, invalid_request, invalid_cursor, precondition_failed, workspace_activation_required, resource_conflict, upstream_error] }
  - { id: session_files_live_stat, method: POST, path: "/api/sessions/{sessionId}/files/live/stat", summary: "Stat one live path without following a symlink, auto-resuming the same generation.", scope: "files:live", request: LiveFileStatRequest, success: LiveFileEntry, safeRetry: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleting, session_deleted, invalid_request, file_not_found, precondition_failed, workspace_activation_required, resource_conflict, upstream_error] }
  - { id: session_files_live_upload_create, method: POST, path: "/api/sessions/{sessionId}/files/live/uploads", summary: "Create a resumable upload in the exact live generation, auto-resuming it when suspended.", scope: "files:live", idempotency: idempotency_key, request: LiveFileUploadCreateRequest, successStatus: 201, success: LiveFileUpload, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleting, session_deleted, idempotency_conflict, invalid_request, precondition_failed, limit_exceeded, workspace_activation_required, resource_conflict, upstream_error, commit_outcome_unknown] }
  - { id: session_files_live_upload_get, method: GET, path: "/api/sessions/{sessionId}/files/live/uploads/{fileUploadId}", summary: "Read resumable live-upload state, auto-resuming the same generation.", scope: "files:live", success: LiveFileUpload, safeRetry: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleting, session_deleted, precondition_failed, workspace_activation_required, resource_conflict, upstream_error] }
  - { id: session_files_live_upload_part_put, method: PUT, path: "/api/sessions/{sessionId}/files/live/uploads/{fileUploadId}/parts/{partNumber}", summary: "Put one exact logical part directly into the retained generation.", scope: "files:live", bodyClass: binary, query: { sha256: { type: content_hash, doc: "SHA-256 of the complete logical part." } }, success: LiveFileUpload, safeRetry: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleting, session_deleted, invalid_request, unsupported_media_type, payload_too_large, precondition_failed, limit_exceeded, workspace_activation_required, resource_conflict, upstream_error] }
  - { id: session_files_live_upload_complete, method: POST, path: "/api/sessions/{sessionId}/files/live/uploads/{fileUploadId}/completion", summary: "Verify every part and atomically publish one live file in the same generation.", scope: "files:live", idempotency: idempotency_key, success: LiveFileUpload, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleting, session_deleted, idempotency_conflict, invalid_request, precondition_failed, content_missing, workspace_activation_required, resource_conflict, upstream_error, commit_outcome_unknown] }
  - { id: session_files_live_upload_delete, method: DELETE, path: "/api/sessions/{sessionId}/files/live/uploads/{fileUploadId}", summary: "Abort a partial upload, auto-resuming the same generation to remove its temporary bytes.", scope: "files:live", pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, not_found, session_deleting, session_deleted, workspace_activation_required, resource_conflict, upstream_error] }
  - { id: session_files_live_download_create, method: POST, path: "/api/sessions/{sessionId}/files/live/downloads", summary: "Open one exact-version live file directly from the retained generation, auto-resuming it.", scope: "files:live", idempotency: idempotency_key, request: LiveFileDownloadRequest, successStatus: 201, success: LiveFileDownload, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleting, session_deleted, idempotency_conflict, invalid_request, file_not_found, precondition_failed, limit_exceeded, workspace_activation_required, resource_conflict, upstream_error, commit_outcome_unknown] }
  - { id: session_files_live_download_part_get, method: GET, path: "/api/sessions/{sessionId}/files/live/downloads/{fileDownloadId}/parts/{partNumber}", summary: "Read one raw exact-version range directly from the retained generation.", scope: "files:live", transport: binary, query: { version: { type: content_hash, doc: "The exact version returned when the download opened." } }, successStatus: 200, safeRetry: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleting, session_deleted, invalid_request, file_not_found, precondition_failed, workspace_activation_required, resource_conflict, upstream_error] }
  - { id: session_files_live_download_complete, method: POST, path: "/api/sessions/{sessionId}/files/live/downloads/{fileDownloadId}/completion", summary: "Re-stat, re-hash, and close an exact live-file download after SDK verification.", scope: "files:live", idempotency: idempotency_key, request: LiveFileDownloadCompleteRequest, success: LiveFileDownload, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleting, session_deleted, idempotency_conflict, invalid_request, precondition_failed, workspace_activation_required, resource_conflict, upstream_error, commit_outcome_unknown] }
  - { id: session_files_live_download_delete, method: DELETE, path: "/api/sessions/{sessionId}/files/live/downloads/{fileDownloadId}", summary: "Close an abandoned live-file descriptor, auto-resuming the same generation.", scope: "files:live", pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, not_found, session_deleting, session_deleted, workspace_activation_required, resource_conflict, upstream_error] }
