plane: regional
fragment: sessions
operations:
  - { id: session_create, method: POST, path: /api/sessions, summary: "Create a session.", scope: "sessions:write", idempotency: idempotency_key, request: SessionCreateRequest, successStatus: 201, success: Session, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, invalid_request, idempotency_conflict, limit_exceeded, unknown_provider, unknown_model, unqualified_provider_model, provider_credential_not_found, provider_credential_revoked, invalid_network_policy, unsupported_package_ecosystem, workspace_activation_required] }
  - { id: sessions_list, method: GET, path: /api/sessions, summary: "List sessions in the workspace.", scope: "sessions:read", success: SessionListPage, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, invalid_cursor], query: { status: { type: ref, to: SessionStatus, optional: true, doc: "Restrict to one status." }, cursor: { type: cursor, optional: true, doc: "Opaque continuation token." }, limit: { type: integer, min: 1, max: 1000, optional: true, doc: "Page size." } } }
  - { id: session_get, method: GET, path: "/api/sessions/{sessionId}", summary: "Read one session, its deleting resource, or its tombstone.", scope: "sessions:read", etag: returns, success: Session, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleted] }
  - { id: session_messages_list, method: GET, path: "/api/sessions/{sessionId}/messages", summary: "List the messages of a session.", scope: "sessions:read", success: MessagePage, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleted, invalid_cursor], query: { cursor: { type: cursor, optional: true, doc: "Opaque continuation token." }, limit: { type: integer, min: 1, max: 1000, optional: true, doc: "Page size." } } }
  - { id: session_message_send, method: POST, path: "/api/sessions/{sessionId}/messages", summary: "Admit a message and queue or start its run.", scope: "sessions:write", idempotency: idempotency_key, request: MessageSendRequest, successStatus: 201, success: MessageSendResult, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleting, session_deleted, invalid_request, idempotency_conflict, limit_exceeded, unqualified_provider_model, provider_credential_revoked] }
  - { id: session_runs_list, method: GET, path: "/api/sessions/{sessionId}/runs", summary: "List the runs of a session.", scope: "sessions:read", success: RunPage, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleted, invalid_cursor], query: { cursor: { type: cursor, optional: true, doc: "Opaque continuation token." }, limit: { type: integer, min: 1, max: 1000, optional: true, doc: "Page size." } } }
  - { id: session_run_get, method: GET, path: "/api/sessions/{sessionId}/runs/{runId}", summary: "Read one run.", scope: "sessions:read", success: Run, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleted] }
  - { id: session_stop, method: POST, path: "/api/sessions/{sessionId}/stops", summary: "Admit the durable stop operation.", scope: "sessions:write", idempotency: operation_id, request: EmptyRequest, successStatus: 202, success: Operation, pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, not_found, session_deleting, session_deleted, operation_idempotency_conflict] }
  - { id: session_persist, method: POST, path: "/api/sessions/{sessionId}/persists", summary: "Admit the durable persist operation.", scope: "files:write", idempotency: operation_id, etag: optional_if_match, request: SessionPersistRequest, successStatus: 202, success: Operation, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleting, session_deleted, invalid_request, invalid_file_selection, operation_idempotency_conflict, precondition_failed, limit_exceeded] }
  - { id: session_fork, method: POST, path: "/api/sessions/{sessionId}/forks", summary: "Admit the durable fork operation.", scope: "sessions:write", idempotency: operation_id, etag: optional_if_match, request: SessionForkRequest, successStatus: 202, success: Operation, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleting, session_deleted, invalid_request, operation_idempotency_conflict, precondition_failed, limit_exceeded] }
  - { id: session_workspace_discard, method: POST, path: "/api/sessions/{sessionId}/workspace/discards", summary: "Admit the durable workspace-discard operation.", scope: "sessions:write", idempotency: operation_id, request: SessionWorkspaceDiscardRequest, successStatus: 202, success: Operation, pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, not_found, session_deleting, session_deleted, invalid_request, operation_idempotency_conflict, precondition_failed] }
  - { id: session_credential_rebind, method: POST, path: "/api/sessions/{sessionId}/credential-rebinds", summary: "Admit the durable credential-rebind operation.", scope: "secrets:write", idempotency: operation_id, request: SessionCredentialRebindRequest, successStatus: 202, success: Operation, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleting, session_deleted, invalid_request, operation_idempotency_conflict] }
  - { id: session_delete, method: POST, path: "/api/sessions/{sessionId}/deletions", summary: "Admit the durable session-deletion operation.", scope: "sessions:delete", idempotency: operation_id, request: SessionDeleteRequest, successStatus: 202, success: Operation, pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, not_found, session_deleted, invalid_request, operation_idempotency_conflict, deletion_in_progress] }
