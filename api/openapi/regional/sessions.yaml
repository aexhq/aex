plane: regional
fragment: sessions
operations:
  - { id: session_create, method: POST, path: /api/sessions, summary: "Create a durable session and eagerly prepare its default-on sandbox in the background.", scope: "sessions:write", idempotency: idempotency_key, request: SessionCreateRequest, successStatus: 201, success: Session, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, invalid_request, idempotency_conflict, limit_exceeded, not_found, unknown_provider, unknown_model, invalid_network_policy, workspace_activation_required, commit_outcome_unknown] }
  - { id: sessions_list, method: GET, path: /api/sessions, summary: "List sessions in the workspace.", scope: "sessions:read", success: SessionListPage, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, invalid_cursor], query: { status: { type: ref, to: SessionStatus, optional: true, doc: "Restrict to one status." }, cursor: { type: cursor, optional: true, doc: "Opaque continuation token." }, limit: { type: integer, min: 1, max: 100, optional: true, doc: "Page size." } } }
  - { id: session_get, method: GET, path: "/api/sessions/{sessionId}", summary: "Read durable session metadata and sandbox preparation state.", scope: "sessions:read", etag: returns, success: Session, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleted] }
  - { id: session_messages_list, method: GET, path: "/api/sessions/{sessionId}/messages", summary: "List complete committed messages in immutable seal order.", scope: "sessions:read", success: MessagePage, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleted, invalid_cursor], query: { cursor: { type: cursor, optional: true, doc: "Opaque continuation token." }, limit: { type: integer, min: 1, max: 100, optional: true, doc: "Page size." } } }
  - { id: session_message_send, method: POST, path: "/api/sessions/{sessionId}/messages", summary: "Admit one text message; file paths are referenced in text, never attached.", scope: "sessions:write", idempotency: idempotency_key, request: MessageSendRequest, successStatus: 201, success: MessageSendResult, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleting, session_deleted, invalid_request, idempotency_conflict, limit_exceeded, session_not_idle, session_terminated, commit_outcome_unknown] }
  - { id: session_messages_stream, method: GET, path: "/api/sessions/{sessionId}/messages/stream", summary: "Stream bounded assistant previews plus commit/reconcile/gap frames.", scope: "sessions:read", transport: ndjson, success: MessageStreamFrame, pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, not_found, session_deleted, invalid_cursor, upstream_error], query: { after: { type: decimal, optional: true, doc: "Resume after this stream sequence." } } }
  - { id: session_cancel, method: POST, path: "/api/sessions/{sessionId}/cancellations", summary: "Cancel current work and return the session to idle.", scope: "sessions:write", idempotency: operation_id, request: EmptyRequest, successStatus: 202, success: SessionCommandReceipt, pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, not_found, session_deleting, session_deleted, operation_idempotency_conflict, commit_outcome_unknown] }
  - { id: session_terminate, method: POST, path: "/api/sessions/{sessionId}/terminations", summary: "Destroy sandbox compute while retaining session metadata and messages.", scope: "sessions:write", idempotency: operation_id, request: EmptyRequest, successStatus: 202, success: SessionCommandReceipt, pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, not_found, session_deleting, session_deleted, operation_idempotency_conflict, commit_outcome_unknown] }
  - { id: session_delete, method: POST, path: "/api/sessions/{sessionId}/deletions", summary: "Irreversibly delete session-scoped user content; independent workspace files remain.", scope: "sessions:delete", idempotency: operation_id, request: EmptyRequest, successStatus: 202, success: SessionCommandReceipt, pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, not_found, session_deleted, operation_idempotency_conflict, deletion_in_progress, commit_outcome_unknown] }
  - { id: session_telemetry_stream, method: GET, path: "/api/sessions/{sessionId}/telemetry/stream", summary: "Stream live trusted assistant, tool, runtime, Logs and Traces telemetry with bounded previews.", scope: "sessions:read", transport: ndjson, success: TelemetryFrame, pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, not_found, session_deleted, invalid_cursor, upstream_error], query: { after: { type: decimal, optional: true, doc: "Resume after this live sequence." } } }
  - { id: session_telemetry_replay, method: GET, path: "/api/sessions/{sessionId}/telemetry/replay", summary: "Replay retained telemetry from compressed immutable S3 segments.", scope: "sessions:read", transport: ndjson, success: TelemetryFrame, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleted, invalid_cursor, upstream_error], query: { after: { type: decimal, optional: true, doc: "Replay after this sequence." }, limit: { type: integer, min: 1, max: 10000, optional: true, doc: "Maximum frames." } } }
  - { id: session_telemetry_download_create, method: POST, path: "/api/sessions/{sessionId}/telemetry/downloads", summary: "Mint a short-lived download for a bounded retained telemetry export.", scope: "sessions:read", idempotency: idempotency_key, request: TelemetryDownloadRequest, successStatus: 201, success: TelemetryDownloadGrant, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleted, invalid_request, idempotency_conflict, upstream_error] }
