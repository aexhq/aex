plane: regional
fragment: sessions
operations:
  - { id: session_create, method: POST, path: /api/sessions, summary: "Create an eight-hour multi-turn session.", scope: "sessions:write", idempotency: idempotency_key, request: SessionCreateRequest, successStatus: 201, success: Session, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, invalid_request, idempotency_conflict, limit_exceeded, unknown_provider, unknown_model, unqualified_provider_model, provider_credential_not_found, provider_credential_revoked, invalid_network_policy, unsupported_package_ecosystem, workspace_activation_required, commit_outcome_unknown] }
  - { id: sessions_list, method: GET, path: /api/sessions, summary: "List sessions in the workspace.", scope: "sessions:read", success: SessionListPage, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, invalid_cursor], query: { status: { type: ref, to: SessionStatus, optional: true, doc: "Restrict to one status." }, cursor: { type: cursor, optional: true, doc: "Opaque continuation token." }, limit: { type: integer, min: 1, max: 100, optional: true, doc: "Page size." } } }
  - { id: session_get, method: GET, path: "/api/sessions/{sessionId}", summary: "Read session metadata, including automatic lifecycle state, or its minimal deletion tombstone.", scope: "sessions:read", etag: returns, success: Session, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleted] }
  - { id: session_messages_list, method: GET, path: "/api/sessions/{sessionId}/messages", summary: "List complete sealed messages in immutable seal-visibility order.", scope: "sessions:read", success: MessagePage, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleted, invalid_cursor], query: { cursor: { type: cursor, optional: true, doc: "Opaque continuation token." }, limit: { type: integer, min: 1, max: 100, optional: true, doc: "Page size." } } }
  - { id: session_message_send, method: POST, path: "/api/sessions/{sessionId}/messages", summary: "Send text and start the session's next work, automatically resuming the same suspended generation.", scope: "sessions:write", idempotency: idempotency_key, request: MessageSendRequest, successStatus: 201, success: MessageSendResult, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleting, session_deleted, invalid_request, idempotency_conflict, limit_exceeded, session_not_idle, session_terminated, unqualified_provider_model, provider_credential_revoked, commit_outcome_unknown] }
  - { id: session_cancel, method: POST, path: "/api/sessions/{sessionId}/cancellations", summary: "Cancel current work and return the session to idle.", scope: "sessions:write", idempotency: operation_id, request: EmptyRequest, successStatus: 202, success: Operation, pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, not_found, session_deleting, session_deleted, operation_idempotency_conflict, commit_outcome_unknown] }
  - { id: session_suspend, method: POST, path: "/api/sessions/{sessionId}/suspensions", summary: "Suspend an idle session while retaining its exact generation; cancel active work first.", scope: "sessions:write", idempotency: operation_id, request: EmptyRequest, successStatus: 202, success: Operation, pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, not_found, session_deleting, session_deleted, operation_idempotency_conflict, session_not_idle, session_terminated, commit_outcome_unknown] }
  - { id: session_resume, method: POST, path: "/api/sessions/{sessionId}/resumptions", summary: "Resume the same retained generation to idle.", scope: "sessions:write", idempotency: operation_id, request: EmptyRequest, successStatus: 202, success: Operation, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, session_deleting, session_deleted, operation_idempotency_conflict, session_terminated, commit_outcome_unknown] }
  - { id: session_terminate, method: POST, path: "/api/sessions/{sessionId}/terminations", summary: "Permanently destroy compute and live files while retaining metadata and sealed messages.", scope: "sessions:write", idempotency: operation_id, request: EmptyRequest, successStatus: 202, success: Operation, pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, not_found, session_deleting, session_deleted, operation_idempotency_conflict, commit_outcome_unknown] }
  - { id: session_delete, method: POST, path: "/api/sessions/{sessionId}/deletions", summary: "Irreversibly delete the session and session-scoped user content, observations, telemetry, and export objects. Independent registered workspace files remain.", scope: "sessions:delete", idempotency: operation_id, request: EmptyRequest, successStatus: 202, success: Operation, pauseExempt: true, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, not_found, session_deleted, operation_idempotency_conflict, deletion_in_progress, commit_outcome_unknown] }
