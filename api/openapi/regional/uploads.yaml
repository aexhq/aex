plane: regional
fragment: uploads
operations:
  - { id: upload_create, method: POST, path: /api/uploads, summary: "Admit a direct upload for one current workspace-file name and return every bounded part grant.", scope: "resources:write", idempotency: idempotency_key, request: UploadCreateRequest, successStatus: 201, success: UploadAdmission, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, invalid_request, idempotency_conflict, limit_exceeded] }
  - { id: upload_complete, method: POST, path: "/api/uploads/{uploadId}/completions", summary: "Verify an admitted upload and publish it only if its private overwrite intent is still current.", scope: "resources:write", idempotency: idempotency_key, request: UploadCompleteRequest, success: RegisteredFile, errors: [unauthenticated, insufficient_scope, wrong_workspace_region, account_paused, not_found, gone, invalid_request, idempotency_conflict, content_missing] }
