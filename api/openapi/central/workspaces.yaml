plane: central
fragment: workspaces
operations:
  - { id: workspaces_list, method: GET, path: /api/workspaces, summary: "List workspaces the caller can reach.", scope: "workspaces:read", success: WorkspacePage, errors: [unauthenticated, insufficient_scope, invalid_cursor], query: { organizationId: { type: id, of: organization, optional: true, doc: "Restrict to one organization." }, cursor: { type: cursor, optional: true, doc: "Opaque continuation token." }, limit: { type: integer, min: 1, max: 1000, optional: true, doc: "Page size." } } }
  - { id: workspace_create, method: POST, path: /api/workspaces, summary: "Create a region-pinned workspace.", scope: "workspaces:write", idempotency: idempotency_key, request: WorkspaceCreateRequest, successStatus: 201, success: Workspace, errors: [unauthenticated, forbidden, insufficient_scope, invalid_request, idempotency_conflict, limit_exceeded, upstream_error] }
  - { id: workspace_get, method: GET, path: "/api/workspaces/{workspaceId}", summary: "Read one workspace.", scope: "workspaces:read", success: Workspace, errors: [unauthenticated, forbidden, insufficient_scope, not_found, gone] }
  - { id: workspace_delete, method: POST, path: "/api/workspaces/{workspaceId}/deletions", summary: "Admit the global workspace-deletion operation.", scope: "workspaces:delete", idempotency: operation_id, request: WorkspaceDeleteRequest, successStatus: 202, success: Operation, pauseExempt: true, errors: [unauthenticated, forbidden, insufficient_scope, not_found, gone, invalid_request, operation_idempotency_conflict, deletion_in_progress] }
