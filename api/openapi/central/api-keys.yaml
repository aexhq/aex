plane: central
fragment: api-keys
operations:
  - { id: api_keys_list, method: GET, path: /api/api-keys, summary: "List workspace API key metadata.", scope: "api_keys:read", success: ApiKeyPage, errors: [unauthenticated, forbidden, insufficient_scope, invalid_request, invalid_cursor], query: { workspaceId: { type: id, of: workspace, doc: "The workspace whose keys are listed." }, cursor: { type: cursor, optional: true, doc: "Opaque continuation token." }, limit: { type: integer, min: 1, max: 1000, optional: true, doc: "Page size." } } }
  - { id: api_key_create, method: POST, path: /api/api-keys, summary: "Mint a workspace API key whose value is returned once.", scope: "api_keys:write", idempotency: idempotency_key, request: ApiKeyCreateRequest, successStatus: 201, success: NewApiKey, errors: [unauthenticated, forbidden, insufficient_scope, not_found, invalid_request, idempotency_conflict, limit_exceeded] }
  - { id: api_key_revoke, method: DELETE, path: "/api/api-keys/{apiKeyId}", summary: "Revoke a workspace API key.", scope: "api_keys:write", etag: optional_if_match, pauseExempt: true, errors: [unauthenticated, forbidden, insufficient_scope, not_found, precondition_failed] }
