# `api/generated/`

The checked-in canonical contract bundle and the language-neutral corpus.

Produced by `tools/aex-contract-gen`. Regeneration must be byte-identical on a
clean tree; a diff here is a build failure, not a formatting preference.
