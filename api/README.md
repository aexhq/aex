# `api/`

Domain-owned contract source. `openapi/` holds OpenAPI 3.1 fragments, `schemas/`
holds JSON Schema 2020-12 documents and semantic registries, and `generated/`
holds the checked canonical bundle plus the language-neutral corpus.

Owner: the contracts stream. Generated content is produced by
`tools/aex-contract-gen` and is never hand-edited.
