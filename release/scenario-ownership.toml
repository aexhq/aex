# The only explicit edge file in the delivery graph.
#
# Every other edge is derived from a manifest that already declares it. These
# cannot be: a scenario exercises a deployable through its public surface
# without importing it, so no dependency edge exists to derive.
#
# A deployable observed by no scenario fails `graph verify`. Observation alone
# is not runnable evidence: each scenario must also declare `package` as a
# namespaced Cargo/npm graph node and `target` as an exact `aex.targets` key
# mapped to the `e2e` layer,
# and that package must claim the scenario in `aex.scenarios`. A scenario whose
# production composition is not runnable instead carries an exact non-empty
# `deferred` reason. Deferral is architecture debt, never evidence: it is shown
# in the graph summary and omitted from the runnable scenario matrix. An
# implicit gap, a partial package/target pair, or runnable+deferred conflict is
# still a graph failure.
#
# --- provisioning in `prd` (OD-35 / OD-36) -----------------------------------
#
# A scenario with no `[scenario.prd]` block runs in `dev` and provisions nothing
# in production. That is the default and it covers everything whose value is
# coverage rather than "does production work": permutation and matrix breadth,
# fuzz, load, soak, provider conformance matrices, hostile workloads and the
# migration rehearsal.
#
# A `[scenario.prd]` block is a claim, checked mechanically by `graph verify`:
#
#   * `rule` must be one of `[prd_provisioning.rule]` in
#     `release/policy/test-profiles.toml`, and the four `exactly_one` rules
#     admit exactly one scenario each;
#   * every kind in `provisions` must be a `[janitor.resource]` row that the
#     janitor can reclaim from tags. A prd-eligible scenario that creates an
#     unreclaimable kind fails `scenario-prd-unreclaimable`;
#   * a kind with `requires_declared_with` drags its companions in. Declaring
#     `payment_customer` without `payment_instrument` and
#     `auto_recharge_policy` fails, because that omission is precisely how a
#     saved card and an enabled auto-recharge policy survived every money-path
#     run in production.

schema = "aex.scenario-ownership.v1"

# --- identity, authorization and control -------------------------------------

[[scenario]]
id = "SC-IDENTITY-ACCOUNT"
owner = "central-identity"
deferred = "No executable live target yet proves the complete account identity and operational-state flow."
observes = ["artifact:central-identity-api"]

[scenario.prd]
rule = "deployable_write"
provisions = [
  "organization",
  "api_key",
]

[[scenario]]
id = "SC-AUTHZ-ASSERTION"
owner = "central-identity"
deferred = "No executable live target yet proves assertion issuance, verification, and revocation together."
observes = ["artifact:central-authz"]

[[scenario]]
id = "SC-CONTROL-WORKSPACE"
owner = "central-identity"
deferred = "The multi-deployable workspace provisioning scenario has no executable live target yet."
observes = [
  "artifact:central-control-api",
  "artifact:central-control-worker",
  "artifact:regional-capacity-controller",
  "artifact:regional-control",
]

[scenario.prd]
rule = "deployable_write"
provisions = [
  "organization",
  "workspace",
  "api_key",
]

[[scenario]]
id = "SC-SCHEMA-MIGRATE"
owner = "central-finance"
deferred = "The central schema administrator companion has no executable migration-rehearsal target yet."
observes = ["artifact:central-schema-admin"]

# --- money --------------------------------------------------------------------

[[scenario]]
id = "SC-FINANCE-INVOICE"
owner = "central-finance"
deferred = "The invoice and settlement composition has no executable live target yet."
observes = ["artifact:finance-api", "artifact:finance-settlement-worker"]

[[scenario]]
id = "SC-FINANCE-PAYMENT"
owner = "central-finance"
deferred = "The end-to-end Stripe command, webhook, and ingest path has no executable live target yet."
# Both ends of one payment: the webhook edge is where a Stripe event enters and
# `finance-ingest` is where it becomes a finance transition, and the command edge
# is where an admitted effect leaves. A scenario that watched only the middle
# would pass while either edge was broken.
observes = [
  "artifact:finance-ingest",
  "artifact:stripe-command-edge",
  "artifact:stripe-webhook-edge",
]

[scenario.prd]
rule = "money_path"
provisions = [
  "organization",
  "workspace",
  "api_key",
  "payment_customer",
  "payment_instrument",
  "auto_recharge_policy",
]

[[scenario]]
id = "SC-FINANCE-RECONCILE"
owner = "central-finance"
deferred = "The finance and provider-cost reconciliation pair has no executable live target yet."
observes = ["artifact:finance-reconcile", "artifact:provider-cost-reconciler"]

[[scenario]]
id = "SC-USAGE-SETTLE"
owner = "observations-usage"
deferred = "The receipt, storage, compute, transfer, and query settlement chain has no executable live target yet."
observes = [
  "artifact:usage-receipt-dispatcher",
  "artifact:usage-storage-worker",
  "artifact:usage-compute-worker",
  "artifact:usage-transfer-worker",
  "artifact:session-stream-api",
]

# --- regional admission and session lifecycle ---------------------------------

[[scenario]]
id = "SC-REGIONAL-ADMISSION"
owner = "regional-services"
package = "cargo:aex-live-session-stream-api"
target = "e2e"
observes = ["artifact:session-stream-api"]

[[scenario]]
id = "SC-SESSION-ADMIT"
owner = "regional-services"
deferred = "Session admission is not mounted by the production composition and has no executable live target yet."
observes = ["artifact:session-stream-api"]

[[scenario]]
id = "SC-SESSION-TERMINAL"
owner = "regional-services"
deferred = "The complete session terminal-operation lifecycle has no executable live target yet."
observes = ["artifact:session-stream-api", "artifact:session-operation-worker"]

[scenario.prd]
rule = "session_lifecycle"
provisions = [
  "organization",
  "workspace",
  "api_key",
  "session",
  "operation",
  "ecs_task",
  "hands_generation",
  "dynamo_item",
]

[[scenario]]
id = "SC-SESSION-STREAM"
owner = "regional-services"
deferred = "The regional streaming transport has no executable scenario target yet."
observes = ["artifact:session-stream-api"]

[[scenario]]
id = "SC-SECRET-CUSTODY"
owner = "regional-stores"
deferred = "The first-seal, key-administration, and custody lifecycle has no executable live target yet."
observes = [
  "artifact:regional-secret-api",
  "artifact:regional-secret-key-admin",
  "artifact:session-stream-api",
]

[scenario.prd]
rule = "secret_path"
provisions = [
  "organization",
  "workspace",
  "api_key",
  "secret_generation",
  "kms_key",
]

[[scenario]]
id = "SC-CONTENT-LIFECYCLE"
owner = "regional-stores"
deferred = "The complete upload, persisted-content, and garbage-collection lifecycle has no executable live target yet."
observes = ["artifact:content-lifecycle-worker", "artifact:session-stream-api"]

[scenario.prd]
rule = "content_path"
provisions = [
  "organization",
  "workspace",
  "api_key",
  "s3_multipart",
  "s3_object",
  "dynamo_item",
]

[[scenario]]
id = "SC-RUNTIME-CONTROL"
owner = "hands"
deferred = "The runtime-control production lifecycle has no executable live target yet."
observes = ["artifact:runtime-control-worker"]

# --- observation ----------------------------------------------------------------

[[scenario]]
id = "SC-OBSERVATION-ADMIT"
owner = "observations-usage"
deferred = "The production OTLP admission path has no executable live target yet."
observes = ["artifact:regional-otlp"]

[scenario.prd]
rule = "deployable_write"
provisions = [
  "organization",
  "workspace",
  "api_key",
  "dynamo_item",
]

[[scenario]]
id = "SC-OBSERVATION-QUERY"
owner = "observations-usage"
deferred = "The regional observation query surface has no executable live target yet."
observes = ["artifact:regional-observation-api"]

[[scenario]]
id = "SC-OBSERVATION-EXPORT"
owner = "observations-usage"
deferred = "The export admission, launcher, and task chain has no executable live target yet."
observes = [
  "artifact:regional-observation-api",
  "artifact:observation-export-launcher",
  "artifact:observation-export-task",
]

[[scenario]]
id = "SC-OBSERVATION-GAP"
owner = "observations-usage"
deferred = "The gap detection, reconciliation, and query lifecycle has no executable live target yet."
observes = ["artifact:observation-reconciler", "artifact:regional-observation-api"]

# --- runtime ---------------------------------------------------------------------

[[scenario]]
id = "SC-BRAIN-TURN"
owner = "brain-core"
deferred = "No executable live target yet proves a complete Brain turn across mux and session authority."
observes = ["artifact:brain-mux", "artifact:session-stream-api"]

[[scenario]]
id = "SC-HANDS-HOSTILE"
owner = "hands"
deferred = "The hostile-workload matrix across Hands agent and all published image variants has no executable target yet. Browser variants remain deferred until their pinned ARM64 browser layer exists."
observes = [
  "artifact:hands-agent",
  "artifact:hands-image-512mb",
  "artifact:hands-image-1gb",
  "artifact:hands-image-2gb",
  "artifact:hands-image-4gb",
  "artifact:hands-image-8gb",
]

# --- published packages -----------------------------------------------------------

[[scenario]]
id = "SC-SDK-INSTALL"
owner = "clients"
deferred = "The packed-tarball clean install proves the archive, not the registry. Installing `@aexhq/sdk` from the real registry and verifying its published provenance needs a version that is already published, so this becomes runnable only after the first release, not before it."
observes = ["artifact:sdk"]
