# Session-centered MVP release registry.
#
# This is deliberately the complete product graph, not a compatibility graph.
# Ten rows are placed compute. The guest agent, its single general-purpose
# image, the dashboard build output, and the published SDK are artifacts but do
# not add a separately scheduled control-plane service.

schema = "aex.units.v1"

id = "central-schema-admin"
kind = "rust-oci-task"
plane = "central"
package = "central-schema-admin"
bin = "central-schema-admin"
extra_inputs = ["bundle:migration"]
target = "aarch64-unknown-linux-gnu.2.34"
profile = "release"
form = "oci-image"
base_image = "public.ecr.aws/amazonlinux/amazonlinux:2023-minimal@sha256:f536dd770d9f7065461c786418265a13f5ba548a11a57bbbf360e677b811ed39"
config_env_namespace = "AEX_SCHEMA_ADMIN_"
config_schema_version = 1
required_receipts = ["unit", "integration", "lint", "package-integrity"]
alarm_spec = "central-schema-admin"
live_suite = "aex-live-central-schema-admin"

# A one-shot task the release engine runs once per release, so `desired_count`
# is 0: nothing is long-lived, and `infra/modules/ecs-task-oneshot` registers a
# task definition with no service behind it. `desired_count = 0` and `port = 0`
# are not choices  `graph verify` rejects anything else for `rust-oci-task`.
#
# `regional-secret-key-admin`'s 512/1024 is the right size, and re-deriving from
# this unit's own work agrees. Unlike every other central unit this one opens a
# direct PostgreSQL wire connection rather than using the Data API, and the whole
# working set is the migration bundle EMBEDDED IN THE BINARY at compile time
# (measured: 14 files, 70,006 bytes; largest single file 15,888 bytes), one
# connection with its TLS stack, and one statement's result  the expensive
# subcommand returns single scalars, roughly a thousand sequential trivially
# cheap round-trips with no parallelism anywhere. 1024 MiB is ~14,000x the
# bundle. 512 cpu is 0.5 vCPU and buys the TLS handshake and statement rendering;
# nothing on the critical path is CPU-bound, so raising it would shorten nothing.
#
# The stop timeout is the family's longest, because the one thing that must never
# be killed mid-flight is a migration inside its transaction. 120 is also the
# module's hard ceiling and is externally pinned: the release launcher fails the
# run if the registered task definition's container stop timeout is not 120.
#
# KNOWN BOUND MISMATCH: the launcher runs `migrate` with a 900,000 ms statement
# timeout against this 120 s stop timeout, which cannot be raised. A statement
# running at its full declared budget is SIGKILLed ~13 minutes before it could
# finish. Either the statement timeout comes down to something a 120 s drain can
# protect, or the launcher is honest that a long migration is not drain-protected.
# Owned by the central-finance / schema stream.
[unit.fargate]
cpu = 512
memory_mb = 1024
desired_count = 0
stop_timeout_s = 120
port = 0

# --- regional plane: request APIs ---------------------------------------------

# `regional-session-api` and `regional-stream` merged here. Both were long-lived
# regional request-path edges on the same plane with the same task shape, the
# same stop timeout, the same `central-authz` dependency and the same authority
# tables, and each carried a production floor of two tasks. One deployable makes
# that floor two tasks instead of four.
#
# The merge is a deployment change and not a contract change: `RouteOwner::SessionApi`
# and `RouteOwner::Stream` both survive, the generated `servingArtifact` strings
# are untouched, and the two route sets are mounted separately and joined with
# `Router::merge` over URL prefixes that were already disjoint. Splitting them
# back apart needs a new unit row and nothing else.
#
# `required_receipts` is the union of the two rows it replaces: the session half
# owed `conformance` and the stream half owed `property`, and merging the
# deployables must not quietly retire either.

[[unit]]
id = "control-api"
kind = "rust-oci-service"
plane = "central"
package = "central-api"
bin = "central-api"
target = "aarch64-unknown-linux-gnu.2.34"
profile = "release"
form = "oci-image"
base_image = "public.ecr.aws/amazonlinux/amazonlinux:2023-minimal@sha256:f536dd770d9f7065461c786418265a13f5ba548a11a57bbbf360e677b811ed39"
config_env_namespace = "AEX_CENTRAL_API_"
config_schema_version = 1
health_path = "/internal/healthz"
ready_path = "/internal/readyz"
required_receipts = ["unit", "contract", "property", "lint", "package-integrity"]
alarm_spec = "control-api"
live_suite = "aex-live-control-api"

[unit.fargate]
cpu = 1024
memory_mb = 2048
desired_count = 2
stop_timeout_s = 30
port = 8080

[[unit]]
id = "control-projection-worker"
kind = "rust-lambda"
plane = "central"
package = "central-control-worker"
bin = "central-control-worker"
target = "aarch64-unknown-linux-gnu.2.34"
profile = "release-lambda"
form = "zip"
entrypoint = "bootstrap"
config_env_namespace = "AEX_CENTRAL_CONTROL_WORKER_"
config_schema_version = 1
required_receipts = ["unit", "lint", "package-integrity"]
alarm_spec = "control-projection-worker"
live_suite = "aex-live-control-projection-worker"

[unit.lambda]
memory_mb = 512
timeout_s = 120
reserved_concurrency = 10

[[unit]]
id = "stripe-webhook-edge"
kind = "ts-lambda"
plane = "central"
package = "@aexhq/stripe-webhook-edge"
target = "none"
profile = "release"
form = "zip"
entrypoint = "handler.js"
config_env_namespace = "AEX_STRIPE_WEBHOOK_"
config_schema_version = 1
required_receipts = ["unit", "contract", "lint", "package-integrity"]
alarm_spec = "stripe-webhook-edge"
live_suite = "aex-live-stripe-webhook-edge"

[unit.lambda]
memory_mb = 512
timeout_s = 30
reserved_concurrency = 40

[[unit]]
id = "billing-worker"
kind = "rust-lambda"
plane = "central"
package = "finance-ingest"
bin = "finance-ingest"
target = "aarch64-unknown-linux-gnu.2.34"
profile = "release-lambda"
form = "zip"
entrypoint = "bootstrap"
config_env_namespace = "AEX_BILLING_WORKER_"
config_schema_version = 1
required_receipts = ["unit", "contract", "property", "lint", "package-integrity"]
alarm_spec = "billing-worker"
live_suite = "aex-live-billing-worker"

[unit.lambda]
memory_mb = 1024
timeout_s = 300
reserved_concurrency = 40

[[unit]]
id = "session-api"
kind = "rust-oci-service"
plane = "regional"
package = "session-stream-api"
bin = "session-stream-api"
target = "aarch64-unknown-linux-gnu.2.34"
profile = "release"
form = "oci-image"
base_image = "public.ecr.aws/amazonlinux/amazonlinux:2023-minimal@sha256:f536dd770d9f7065461c786418265a13f5ba548a11a57bbbf360e677b811ed39"
config_env_namespace = "AEX_SESSION_API_"
config_schema_version = 1
health_path = "/internal/healthz"
ready_path = "/internal/readyz"
required_receipts = ["unit", "contract", "conformance", "property", "lint", "package-integrity"]
alarm_spec = "session-api"
live_suite = "aex-live-session-api"

[unit.fargate]
cpu = 1024
memory_mb = 2048
desired_count = 2
stop_timeout_s = 30
port = 8080

[[unit]]
id = "file-ingest-worker"
kind = "rust-lambda"
plane = "regional"
package = "file-ingest-worker"
bin = "file-ingest-worker"
target = "aarch64-unknown-linux-gnu.2.34"
profile = "release-lambda"
form = "zip"
entrypoint = "bootstrap"
config_env_namespace = "AEX_FILE_INGEST_"
config_schema_version = 1
required_receipts = ["unit", "property", "lint", "package-integrity"]
alarm_spec = "file-ingest-worker"
live_suite = "aex-live-session-api"

[unit.lambda]
memory_mb = 1024
timeout_s = 300
reserved_concurrency = 20

[[unit]]
id = "brain-mux"
kind = "rust-oci-service"
plane = "runtime"
package = "brain-mux"
bin = "brain-mux"
target = "aarch64-unknown-linux-gnu.2.34"
profile = "release"
form = "oci-image"
base_image = "public.ecr.aws/amazonlinux/amazonlinux:2023-minimal@sha256:f536dd770d9f7065461c786418265a13f5ba548a11a57bbbf360e677b811ed39"
config_env_namespace = "AEX_BRAIN_MUX_"
config_schema_version = 1
health_path = "/internal/healthz"
ready_path = "/internal/readyz"
required_receipts = ["unit", "property", "contract", "lint", "package-integrity"]
alarm_spec = "brain-mux"
live_suite = "aex-live-brain-mux"

[unit.fargate]
cpu = 2048
memory_mb = 4096
desired_count = 2
stop_timeout_s = 120
port = 8080

[[unit]]
id = "tool-mux"
kind = "rust-oci-service"
plane = "runtime"
package = "tool-mux"
bin = "tool-mux"
target = "aarch64-unknown-linux-gnu.2.34"
profile = "release"
form = "oci-image"
base_image = "public.ecr.aws/amazonlinux/amazonlinux:2023-minimal@sha256:f536dd770d9f7065461c786418265a13f5ba548a11a57bbbf360e677b811ed39"
config_env_namespace = "AEX_TOOL_MUX_"
config_schema_version = 1
health_path = "/internal/healthz"
ready_path = "/internal/readyz"
required_receipts = ["unit", "contract", "property", "lint", "package-integrity"]
alarm_spec = "tool-mux"
live_suite = "aex-live-tool-mux"

[unit.fargate]
cpu = 1024
memory_mb = 2048
desired_count = 2
stop_timeout_s = 120
port = 8080

[[unit]]
id = "runtime-control-worker"
kind = "rust-lambda"
plane = "regional"
package = "runtime-control-worker"
bin = "runtime-control-worker"
target = "aarch64-unknown-linux-gnu.2.34"
profile = "release-lambda"
form = "zip"
entrypoint = "bootstrap"
config_env_namespace = "AEX_RUNTIME_CONTROL_"
config_schema_version = 1
required_receipts = ["unit", "property", "lint", "package-integrity"]
alarm_spec = "runtime-control-worker"
live_suite = "aex-live-runtime-control-worker"

[unit.lambda]
memory_mb = 1024
timeout_s = 120
reserved_concurrency = 20

[[unit]]
id = "session-maintenance-worker"
kind = "rust-lambda"
plane = "regional"
package = "session-operation-worker"
bin = "session-operation-worker"
target = "aarch64-unknown-linux-gnu.2.34"
profile = "release-lambda"
form = "zip"
entrypoint = "bootstrap"
config_env_namespace = "AEX_SESSION_MAINTENANCE_"
config_schema_version = 1
required_receipts = ["unit", "property", "lint", "package-integrity"]
alarm_spec = "session-maintenance-worker"
live_suite = "aex-live-session-maintenance-worker"

[unit.lambda]
memory_mb = 1024
timeout_s = 300
reserved_concurrency = 20

[[unit]]
id = "hands-agent"
kind = "rust-binary"
plane = "runtime"
package = "hands-agent"
bin = "hands-agent"
target = "aarch64-unknown-linux-musl"
profile = "release"
form = "tar.gz"
config_env_namespace = "AEX_HANDS_AGENT_"
config_schema_version = 1
required_receipts = ["unit", "property", "contract", "lint", "determinism"]
alarm_spec = "hands-agent"
live_suite = "aex-live-hands-agent"

[[unit]]
id = "hands-image"
kind = "microvm-image"
plane = "runtime"
package = "hands-image"
target = "aarch64-unknown-linux-musl"
profile = "release"
form = "zip"
base_image = "public.ecr.aws/lambda/microvms:al2023-minimal@sha256:05cb9b38d841e7ff1b693dc9e894909612f340bf99ec97d426e8000a5bbe96c3"
config_env_namespace = "AEX_HANDS_IMAGE_"
config_schema_version = 1
required_receipts = ["unit", "lint", "package-integrity", "determinism"]
alarm_spec = "hands-image"
live_suite = "aex-live-hands-image"
extra_inputs = ["cargo:hands-agent"]

[unit.microvm]
variant = "2gb"
minimum_memory_mib = 2048
browser = false

[[unit]]
id = "dashboard"
kind = "build-output"
plane = "web"
package = "@aexhq/dashboard"
target = "none"
profile = "release"
form = "tar.gz"
config_env_namespace = "AEX_"
config_schema_version = 1
required_receipts = ["unit", "lint"]
live_suite = "aex-live-dashboard"

[[unit]]
id = "sdk"
kind = "npm-package"
plane = "public"
package = "@aexhq/sdk"
target = "none"
profile = "release"
form = "npm-tarball"
config_schema_version = 1
required_receipts = ["unit", "contract", "lint", "package-integrity"]
